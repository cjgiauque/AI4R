# from math import*
#
# def f(mu, sigma2, x):
#   return
#
# print(f(10,4,8))

def f(mu, sigma2, x):
    P1 = 1 / math.sqrt(2 * math. * sigma2)
    P2 = math.exp(-1 / 2)
    P3 = ((x - mu) ** 2 / ss)

    return